import React from 'react';

const ProfileDisplay = () => {
    return (
        <div>
            <button>
                Sign In / Login
            </button>
        </div>
    );
};

export default ProfileDisplay;